# Next steps (met la titre comme la description)
- [ ] Mettre à jour `index.html` pour corriger/aligner la zone Titre comme celle de Description (structure, compteur, champ erreur).
- [ ] Mettre à jour `style.css` pour styliser `field-with-counter`/`field-error` de façon identique pour Titre et Description.
- [ ] Vérifier dans `app.js` que la validation temps réel et le compteur du Titre fonctionnent comme la Description (input/blur + messages + bordure success/error).
- [ ] Tester rapidement: saisir moins/plus que min/max sur Titre et Description; vérifier que messages/bordures/compteurs sont cohérents.

