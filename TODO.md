# TODO

- [ ] Mettre à jour `index.html` pour ajouter les compteurs de caractères restants (titre, description) et ajuster `maxlength` description à 255.
- [ ] Mettre à jour `style.css` pour gérer états `field--error`/`field--success` et styliser le compteur.
- [ ] Mettre à jour `app.js` : validation temps réel au `input`/`blur`, messages rouges sous champs, bordures rouges/vertes, logique min/max (titre>=5, description 25..255), compteur restant à jour.
- [ ] Tester manuellement : blur sans saisie, saisir <min/max, corriger => message disparaît, bordure verte.

